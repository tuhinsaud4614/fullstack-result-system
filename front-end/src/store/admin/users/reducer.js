const initialState = {
  data: [],
};

const reducer = (state = initialState, action) => {
  // switch (key) {
  //     case value:

  //         break;

  //     default:
  //         break;
  // }
  return state;
};

export default reducer;
